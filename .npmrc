# This project uses pnpm. Do NOT use npm or yarn.
# Install: npm install -g pnpm  or  corepack enable pnpm
# All commands: pnpm build | pnpm test | pnpm lint

# Only pnpm is allowed (npm will warn but the comment makes it clear)
# engine-strict = true